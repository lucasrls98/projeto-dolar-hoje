{
  "name": "dolar-hoje",
  "version": "1.0.0",
  "description": "A beautiful Vue.js application to display current dollar exchange rate in BRL",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@vitejs/plugin-vue": "^4.5.0",
    "cypress": "^14.5.1",
    "gh-pages": "^6.1.0",
    "vite": "^5.0.0",
    "vitest": "^3.2.4",
    "wait-on": "^7.2.0"
  }
}
